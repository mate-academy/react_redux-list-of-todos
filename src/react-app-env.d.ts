/// <reference types="react-scripts" />

type Todo = {
  id: number,
  userId: number;
  title: string,
  completed: boolean,
};

type User = {
  userId: number,
  name: string
  email: string,
  phone: string,
};
