// eslint-disable-next-line
/// <reference types="react-scripts" />

type Todo = {
  completed: boolean;
  createAt: string;
  id: number;
  title: string;
  updateAt: string;
  userId: number;
};

type User = {
  createdAt: string;
  email: string;
  id: number;
  name: string;
  phone: string;
  updatedAt: string;
  username: string;
  website: string;
};
