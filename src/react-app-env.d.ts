// eslint-disable-next-line
/// <reference types="react-scripts" />

type Todo = {
  completed: boolean;
  id: number;
  title: string;
  userId: number;
};

type User = {
  id: number;
  name: string;
  website: string;
  phone: string;
};
